import * as React from "react";

const Filters = (): JSX.Element => {
  return (
    <div className="fixed bottom-0 left-0 z-20 flex h-16 w-full items-center justify-center border-t bg-white md:hidden">
      SORT / FILTER
    </div>
  );
};

export default Filters;
